<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h3 class="margin_top">送信が完了しました</h3>
      <div class="mt-5">
        <div>内容は以下の通りです。</div>
        <div class="mt-3">
          [タイトル]<p class="font-weight-hold"><%= @mail_title %></p>
        </div>
        <div>
          [本文]<p class="font-weight-bold"><%= safe_join(@mail_content.split("\n"),tag(:br)) %></p>
        </div>
      </div>
      <div class="text-center">
        <%= link_to "グループに戻る", group_path(@group), class: "btn btn-success btn-sm" %>
      </div>
    </div>
  </div>
</div>